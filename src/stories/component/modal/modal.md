
Modal reutilizable que proyecta contenido mediante `ng-content`.

## Uso

En este ejemplo, el modal se abre usando el componente `Button`.

El contenido interno se define desde el componente padre y se proyecta dentro del modal.
